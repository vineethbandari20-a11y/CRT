import cmath
a,b,c=map(int,input().split())
d = b**2 - 4*a*c
match d:
    case _ if d > 0:
        root1 = (-b + d**0.5) / (2*a)
        root2 = (-b - d**0.5) / (2*a)
        print(root1,root2)
    case 0:
        root = -b / (2*a)
        print(root,root)
    case _:
        root1 = (-b + cmath.sqrt(d)) / (2*a)
        root2 = (-b - cmath.sqrt(d)) / (2*a)
        print(root1,root2)
